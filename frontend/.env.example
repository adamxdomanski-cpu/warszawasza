# Browser-facing port (Next.js dev server)
NEXT_PUBLIC_API_PORT=8000

# Optional explicit API URL. Leave empty to use window.location.origin (same port).
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Internal FastAPI port proxied by Next.js rewrites
API_PROXY_TARGET=http://127.0.0.1:8001
