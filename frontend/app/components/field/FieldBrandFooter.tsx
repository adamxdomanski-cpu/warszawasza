/** Discreet mark — after first actions, not before. */
export default function FieldBrandFooter() {
  return (
    <footer className="mt-auto pt-8 text-center">
      <p className="m-0 font-mono-field text-[10px] tracking-[0.35em] text-accent/35 uppercase">
        WARSZAWASZA
      </p>
    </footer>
  );
}
