{
  "name": "kernel pointer",
  "description": "Canonical rules live in .cursor/rules/core.mdc (always). WARSZAWASZA module: .cursor/rules/warszawasza.mdc",
  "rules": [
    "Apply .cursor/rules/core.mdc. For this repo, also apply .cursor/rules/warszawasza.mdc when paths match."
  ]
}
