{
  "name": "BABCIA OS",
  "description": "Heuristic for writing code — not a product. WARSZAWASZA scope: docs/project.md",
  "meta_rule": "Rules are provisional. Reality validates in the field. Repeated friction → improve process before adding rules.",
  "rules": [
    "Filter: Does this help a person make a better decision with less effort? If no — remove it.",
    "Separate observations from hypotheses. Systems register traces; humans decide. Deployment prepares; the field validates.",
    "CANONICAL detail (only when needed): .cursor/rules/core.mdc · Build the product: frontend/ · Field test: /field/heat"
  ]
}
